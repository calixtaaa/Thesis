source venv/bin/activate
export DISPLAY=:0
export PYTHONNOUSERSITE=1

# Make sure I2C is enabled and available
sudo raspi-config nonint do_i2c 0
ls /dev/i2c-1

# Use the addresses actually wired on your machine
# Single expander example:
# export MCP23017_ADDRESSES=0x20
# Three expander example (default full slot map):
export MCP23017_ADDRESSES=0x20,0x21,0x22

# Optional: verify detected addresses before running the UI tester
# i2cdetect -y 1

python stepper_mcp.py
